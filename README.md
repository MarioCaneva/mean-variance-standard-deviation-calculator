# mean-variance-standard-deviation-calculator
A NumPy-based Python project that calculates the mean, variance, standard deviation, max, min, and sum of a 3×3 matrix across rows, columns, and the flattened array. Includes error handling and returns results in a structured dictionary.
